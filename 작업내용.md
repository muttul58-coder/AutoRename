# 파일명 일괄 변경기 - 전체 작업 내용

## 프로젝트 개요

폴더 단위로 파일명을 일괄 변경하는 웹 기반 도구. 행사명·날짜 규칙에 따라 파일을 자동 리네임하며, 브라우저(File System Access API) 기반과 PyInstaller EXE 빌드 두 가지 배포 형태를 지원.

**작업 경로:** `E:\File_Name_Change`

---

## 파일 구성

| 파일 | 역할 |
|------|------|
| [file_renamer.html](file_renamer.html) | 메인 애플리케이션 (HTML/CSS/JS 단일 파일, 1532줄) |
| [server.bat](server.bat) | 로컬 Python HTTP 서버 실행 스크립트 (포트 8080) |
| [file_renamer.spec](file_renamer.spec) | PyInstaller 빌드 설정 (`파일명_일괄변경기.exe`) |
| [README.txt](README.txt) | EXE 빌드 / 사용 안내 |

---

## 주요 기능

### 1. 파일명 변환 규칙
- **폴더명 패턴:** `행사명(YYYY.MM.DD.)` 예) `스승의날(2026.05.15.)`
- **파일명 패턴:** `행사명_번호(YYYY.MM.DD.).확장자` 예) `스승의날_1(2026.05.15.).jpg`
- 하위 폴더마다 **독립 번호** 부여

### 2. UI / UX
- 다크 / 라이트 테마 토글 (Catppuccin 컬러 팔레트)
- 폴더 드래그앤드롭 또는 클릭 선택
- 변환 결과 미리보기 테이블 (변경/동일/오류 색상 구분)
- 다중 루트 폴더 지원 (`renderRootsPanel`)
- 하위 폴더 선택 / 개별 편집 (`renderFolderSelector`, `renderSubfolderEditRow`)
- 연도 입력 모달 + 스테퍼 (`askYear`)
- 충돌 감지 (`detectCollision`)

### 3. 핵심 함수 ([file_renamer.html](file_renamer.html))
| 함수 | 위치 | 역할 |
|------|------|------|
| `supportsMove` | [:301](file_renamer.html:301) | File System Access `move()` 지원 여부 확인 |
| `formatDate` | [:313](file_renamer.html:313) | ISO 날짜 → `YYYY.MM.DD.` 변환 |
| `parseFolderName` | [:324](file_renamer.html:324) | 폴더명에서 행사명·날짜 추출 |
| `splitExt` | [:386](file_renamer.html:386) | 파일명·확장자 분리 |
| `buildPlan` | [:796](file_renamer.html:796) | 변경 계획 생성 |
| `renderTable` | [:944](file_renamer.html:944) | 미리보기 렌더링 |
| `getFullDestPath` | [:1013](file_renamer.html:1013) | 최종 경로 계산 |
| `askYear` | [:1048](file_renamer.html:1048) | 연도 입력 모달 |
| `detectCollision` | [:1393](file_renamer.html:1393) | 파일명 충돌 검사 |

---

## 실행 방법

### A. 브라우저 (권장)
```bat
server.bat
```
→ `http://localhost:8080/file_renamer.html` 자동 열림 (Chrome/Edge 등 File System Access API 지원 브라우저 필요)

### B. EXE 배포
1. Python 설치 (PATH 등록)
2. `빌드_실행.bat` 실행 → `파일명_일괄변경기.exe` 생성
3. 콘솔 없는 GUI 모드(`console=False`), UPX 압축 적용

---

## 사용 절차
1. 프로그램 실행
2. 폴더 드래그앤드롭 또는 클릭 선택
3. 행사명·날짜 자동 파싱 → 미리보기 확인
4. **변경 실행** 버튼 클릭
5. 확인 팝업 → 예
